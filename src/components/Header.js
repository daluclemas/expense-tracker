import React from 'react';
import style from './Header.module.css';

const Header = () => {
    return (
        

        <h1 className={ ` col-12 ${style.header}` }>expense tracker</h1>
       
    )
}

export default Header
